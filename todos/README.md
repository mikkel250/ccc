# Todos

CE `/ce-code-review` receipts. **YAML `status:` is the queue bit**, not the filename.

All files here are closed (`done` / `completed` / `closed` / `complete` / `cancelled`). Do not glob `*-pending-*` as open work. Unchecked `- [ ]` boxes on a closed todo are fossils — not a reopen signal.

Living review leftovers: `docs/residual-review-findings/`.
